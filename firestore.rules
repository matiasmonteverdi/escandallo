rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Flat structure: All documents require authentication for read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
